<h2>商品一覧</h2>
<p>全<%=@items.count %>件</p>

<!--<div class="row">-->
  <% @items.each do |item|  %>
  <!--<div class="col-md-3">-->
     <div>
       <%= link_to "/items/#{item.id}" do %>
        <%= attachment_image_tag item, :image, :fill, 300, 300, format: 'jpeg' %>
       <% end %>
     </div>
     <div>
       <p><%= item.name%></p>
       <p>¥<%=item.price%></p>
     </div>
   <!--</div>-->
  <% end %>
<!--</div>-->
